export default function StorageWarningBanner() {
  return (
    <div className="storage-warning" role="alert">
      <span>
        🧊 Store in freezer immediately upon arrival. Product spoils if left outside freezer for
        more than 3 days.
      </span>
    </div>
  )
}
